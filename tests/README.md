# tests/

Mirrors `src/core/` per `ThaiAI_Architecture.md`. Unit tests land here in
Phase 2 item 9 (parser, storage, search, context, diff), using
`fake-indexeddb` for IndexedDB CRUD tests as specified in the item 9 prompt.
